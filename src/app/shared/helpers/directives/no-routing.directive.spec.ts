import { NoRoutingDirective } from './no-routing.directive';

describe('NoRoutingDirective', () => {
  it('should create an instance', () => {
    const directive = new NoRoutingDirective();
    expect(directive).toBeTruthy();
  });
});
